# Server Configuration
server.port=8080
spring.application.name=flexreact-backend

# Database Configuration (Supabase PostgreSQL)
# Usando conexión directa con usuario postgres
spring.datasource.url=jdbc:postgresql://db.unbyenhdrldevruaomfp.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=UpZcOlAsWVvMOTGK
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=flexreact_super_secret_key_2025_$ecure
jwt.expiration=604800000

# CORS Configuration
cors.allowed.origins=http://localhost:3000

# Logging
logging.level.com.flexreact=DEBUG
logging.level.org.springframework.security=DEBUG
